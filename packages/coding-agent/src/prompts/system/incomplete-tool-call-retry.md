<system-injection>
Your previous tool call was truncated before its arguments were complete, so it was discarded and not executed.

Continue the user's task now. Do not repeat the same large tool-call arguments. Use a lower-output strategy: split generated content into small chunks, prefer edit or patch operations over large write payloads, and run one small tool call at a time.

Attempt #{{retryCount}}/{{maxRetries}}
</system-injection>

<checkpoint-seal-control>
Close the active checkpoint now by calling `seal` once with strategy `summary`.

The structured report must factually capture the completed request-core outcome, durable application-layer APIs and invariants, decisions and reasons, exact observed verification, remaining CLI work, and the immediate continuation phase. Do not read files, run commands, or call any other tool. After the seal completes, respond exactly `SEALED`.
</checkpoint-seal-control>
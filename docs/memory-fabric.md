# Memory Fabric

This standalone proposal is retired.

The canonical design now lives in:

```text
/Users/case/projects/agent-gateway/docs/ARCHITECTURE.md
```

That document contains the integrated architecture for:

- gateway control plane
- OMP runtime driver model
- memory fabric lifecycle
- scoped memory records
- retrieval, inheritance, and active-context compilation
- learning cadence
- agentic/hybrid learning work graphs
- skill, recipe, runbook, and implementation self-improvement
- owner-scoped installation and audit

OMP-local documentation should describe OMP's runtime memory APIs and behavior. Gateway-level memory fabric and self-improvement architecture belong in the gateway architecture document so the design stays coherent instead of split across competing proposals.
